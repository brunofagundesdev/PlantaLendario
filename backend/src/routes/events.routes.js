// import louco dos services pra cada método http
// import do auth e controller
export function eventRoutes(app) {
    app.get("/events", (request, reply) => {
        
    })

    app.get("/events/:id", (request, reply) => {

    })

    app.post("/events", (request, reply) => {

    })

    app.patch("/events/:id", (request, reply) => {
        
    })

    app.put("/events/:id", (request, reply) => {

    })

    app.delete("/events/:id", (request, reply) => {

    })
}