// import louco dos services pra cada método http
// import do auth e controller
export function eventRoutes(app) {
    app.get("/disciplines", (request, reply) => {
        
    })

    app.get("/disciplines/:id", (request, reply) => {

    })

    app.post("/disciplines", (request, reply) => {

    })

    app.patch("/disciplines/:id", (request, reply) => {
        
    })

    app.put("/disciplines/:id", (request, reply) => {

    })

    app.delete("/disciplines/:id", (request, reply) => {

    })
}